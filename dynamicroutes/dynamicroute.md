in the react native we can get the dynamic route by the [id].tsx only

[] refers the dynamic route and likely to be the deep linking.

    // by the below cmd we can get the particular product details.
 => npx uri-scheme open asyncstorage://products/1 --android