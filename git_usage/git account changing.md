git account changing

from one to another

=> login to that account in browser

// first need to check the user name and the email.

	=> git config --get user.name

	=> git config --get user.email

// now change the email and user name as

	=> git config --global user.name "prasad392"

	=> git config --global user.email "Venkata.prasad@gmail.com"

// control panel --> credential manger --> window manager --> remove the GitHub.com